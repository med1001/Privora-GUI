# Site origin only (no /api). Requests use ${REACT_APP_API_URL}/api/...
REACT_APP_API_URL=http://localhost:8000

# WebSocket URL (through nginx in production: wss://your-domain/ws)
REACT_APP_WS_URL=ws://localhost:8000/ws
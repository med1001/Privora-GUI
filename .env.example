# Site origin only (no /api). Prefer 127.0.0.1 on Windows+Docker (localhost IPv6 quirks).
REACT_APP_API_URL=http://127.0.0.1:8000

# WebSocket (runtime apiBase.ts can rewrite host when you open the app by LAN IP)
REACT_APP_WS_URL=ws://127.0.0.1:8000/ws